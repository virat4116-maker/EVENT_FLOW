PORT=5000
JWT_SECRET=eventflow_super_secret_change_me
JWT_EXPIRES_IN=7d
# This demo runs on a local JSON file store (backend/data/db.json) so it works with
# zero external setup. To move to real MongoDB, install mongoose and swap
# backend/data/store.js for the schemas already sketched in backend/models/.
MONGODB_URI=mongodb://localhost:27017/eventflow
